extends layout

block state
    body(class="signIn_body")
        form(class="signIn_form" action="/login/twitter" method="get")
            //- input(type="text")
            h1 Twitter Interface
            button(type="submit" class="signIn_btn") Login